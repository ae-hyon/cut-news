'use client'

export default function KakaoCallbackHandler() {
  // Kakao callback is now handled inline by useKakaoLogin via ?auth=kakao query param.
  // This component is kept as a no-op for backwards compatibility with existing page imports.
  return null
}
